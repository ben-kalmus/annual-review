# Copy this file to .env and fill in your values.
# .env is gitignored — never commit real credentials.

JIRA_URL=https://algolia.atlassian.net
JIRA_EMAIL=you@algolia.com
JIRA_TOKEN=your-api-token-here

# Story points custom field — auto-discovered as customfield_10078 on this instance.
# Pass as --sp-field to fetch_sprint_totals.py to skip re-discovery.
JIRA_SP_FIELD=customfield_10078
